/**
 * 【已废弃 / DEPRECATED】
 *
 * 原「页面内注入式 UI 组件层」（录制面板 / 模态框 / 加载框 / toast）已从链路移除。
 *
 * 原因：`chrome.tabCapture` 捕获的是整个标签页的合成画面，任何注入到页面里的浮层
 * 都会与播放器一起被合成进捕获帧，从而出现在最终视频中；而播放器区域会随全屏、
 * 剧场模式、窗口尺寸变化，「浮层恰好不压到播放器」无法稳定保证。
 *
 * 现状：录制控件（开始 / 停止 / 状态 / 计时 / 提示）全部迁移到扩展图标弹窗
 * `src/popup.html` + `src/popup.js`；popup 是独立的扩展页面，不属于被捕获标签页
 * 的渲染内容，因此绝不会被录进视频。
 *
 * 本文件已不在 `manifest.json` 的 content_scripts 中加载（可安全删除）。
 */
